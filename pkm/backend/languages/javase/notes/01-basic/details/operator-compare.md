# 关系运算符

## 基本关系运算符
- **等于**：`==`
- **不等于**：`!=`
- **小于**：`<`
- **大于**：`>`
- **小于等于**：`<=`
- **大于等于**：`>=`


## 类型检查运算符
- **类型判断**：`instanceof`

```java
String str = "Hello";
System.out.println(str instanceof String);  // true
System.out.println(str instanceof Object);  // true
```
